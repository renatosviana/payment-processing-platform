pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'payment-platform'

include 'payment-service', 'processor-simulator', 'integration-tests'
